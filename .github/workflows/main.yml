name: CI Build
on: [push, pull_request]  # Triggers on push or PR

jobs:
  build:
    runs-on: ubuntu-latest  # Runner OS (Linux, Windows, macOS)
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4  # Checks out your repo
        
      - name: Print a greeting
        run: echo "Hello, GitHub Actions!"